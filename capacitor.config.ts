import type { CapacitorConfig } from "@capacitor/cli"

const config: CapacitorConfig = {
  appId: "com.pwscho.vaultapp",
  appName: "VaultApp 2.0",
  webDir: "out",
  server: {
    androidScheme: "https",
  },
}

export default config
